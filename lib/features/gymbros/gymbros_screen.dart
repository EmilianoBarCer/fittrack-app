import 'package:flutter/material.dart';

class GymbrosScreen extends StatelessWidget {
  const GymbrosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Gymbros'),
      ),
    );
  }
}